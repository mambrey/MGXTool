--- a/src/components/AccountForm.tsx
+++ b/src/components/AccountForm.tsx
@@ -60,7 +60,7 @@
   // Initialize Google Places Autocomplete
   useEffect(() => {
-    if (isLoaded && addressInputRef.current && GOOGLE_MAPS_API_KEY) {
+    if (isLoaded && addressInputRef.current && GOOGLE_MAPS_API_KEY && window.google?.maps?.places?.Autocomplete) {
       try {
         autocompleteRef.current = new google.maps.places.Autocomplete(addressInputRef.current, {
           types: ['address'],
@@ -89,7 +89,7 @@
         });
       } catch (error) {
-        console.error('Error initializing Google Places Autocomplete:', error);
+        console.warn('Google Places Autocomplete not available:', error);
       }
     }
   }, [isLoaded]);
